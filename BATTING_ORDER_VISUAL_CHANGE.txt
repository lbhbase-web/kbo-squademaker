타순 설정 UI를 사용자 제공 참고 화면처럼 변경했습니다.
- 타순 설정을 중앙 모달로 표시
- 1~2번 상위 타선(빨강), 3~5번 클린업(파랑), 6~9번 하위 타선(초록)
- 빈 타순은 + 원형 버튼으로 표시
- 현재 라인업 타자 카드를 아래에 표시
- 아래 타자 카드 선택 → 원하는 타순 칸 선택 방식
- 이미 선수가 있는 타순에 배치하면 기존 선수와 순서 교환
- 태블릿/모바일 터치 조작 대응
